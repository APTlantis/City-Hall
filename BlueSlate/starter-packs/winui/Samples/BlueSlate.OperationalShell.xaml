<Page
    x:Class="Aptlantis.Sample.BlueSlate.OperationalShell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{ThemeResource BlueSlateBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="56" />
            <RowDefinition Height="*" />
            <RowDefinition Height="32" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="{ThemeResource BlueSlatePanelBrush}" BorderBrush="{ThemeResource BlueSlateBorderBrush}" BorderThickness="0,0,0,1">
            <TextBlock Text="Blue-Slate Operational Shell" Foreground="{ThemeResource BlueSlateTextBrush}" VerticalAlignment="Center" Margin="16,0" FontWeight="SemiBold" />
        </Border>
        <Grid Grid.Row="1" ColumnSpacing="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="360" />
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="{ThemeResource BlueSlatePanelBrush}" Padding="16">
                <TextBlock Text="Navigation / Tree" Foreground="{ThemeResource BlueSlateTextSoftBrush}" />
            </Border>
            <Border Grid.Column="1" Background="{ThemeResource BlueSlateBackgroundBrush}" Padding="16">
                <TextBlock Text="Primary Work Area" Foreground="{ThemeResource BlueSlateTextBrush}" />
            </Border>
            <Border Grid.Column="2" Background="{ThemeResource BlueSlatePanelBrush}" Padding="16">
                <TextBlock Text="Evidence / Output" Foreground="{ThemeResource BlueSlateTextSoftBrush}" />
            </Border>
        </Grid>
        <Border Grid.Row="2" Background="{ThemeResource BlueSlatePanelBrush}" BorderBrush="{ThemeResource BlueSlateBorderBrush}" BorderThickness="0,1,0,0">
            <TextBlock Text="Ready" Foreground="{ThemeResource BlueSlateActionBrush}" VerticalAlignment="Center" Margin="16,0" />
        </Border>
    </Grid>
</Page>
