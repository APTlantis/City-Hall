# SESM Suite Map

## Suite Layer

SESM is indexed by `SESM.manifest.toml`, `README.md`, and `SESM-v0.2.md`.

## Adopter Layer

SVG asset adopters use `svg_asset.schema.json`, `templates/SESM-Metadata-Example.json`, and the SESM tooling.

## Validation Layer

Validate metadata with `svg_asset.schema.json` and run `tests/run_tests.py` after tooling changes.
