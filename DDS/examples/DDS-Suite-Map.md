# DDS Suite Map

## Suite Layer

DDS is indexed by `DDS.manifest.toml`, `README.md`, and `Dataset Development Standard.md`.

## Adopter Layer

Dataset adopters use `templates/DATASET.manifest.toml` and `templates/Provenance-Record.md`.

## Validation Layer

Validate provenance, license status, splits, hashes, and known limitations with `Validation-Checklist.md`.
