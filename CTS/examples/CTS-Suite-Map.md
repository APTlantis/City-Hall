# CTS Suite Map

## Suite Layer

CTS is indexed by `CTS.manifest.toml`, `README.md`, and `Command Tool Standard.md`.

## Adopter Layer

CLI adopters use `templates/Command-Contract.md` and `templates/CLI-Release-Checklist.md`.

## Validation Layer

Validate command behavior, exit codes, help output, diagnostics, and automation compatibility with `Validation-Checklist.md`.
